server:
  port: 8080

spring:
  application:
    name: rag-gateway

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
